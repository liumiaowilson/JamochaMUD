Usage is (hopefully) straightforward.  All commands are given using the syntax
'#parse <subcommand> <options>".

This program has allowances for internationalization.  The commands and
output may all be set for different locales, assuming that the proper resource
file is available.  However, the '#parse' command is hardwired into the code.
